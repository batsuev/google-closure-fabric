# Installation

    pip install https://github.com/batsuev/google-closure-fabric/archive/master.zip

# Usage with fabric
Example fabfile.py:

    import google_closure_fabric, os

    def bootstrap():
        google_closure_fabric.bootstrap(os.path.dirname(__file__))

Run with:

    fab bootstrap

It will create google-closure folder with google closure stylesheets, google closure templates and google closure library.